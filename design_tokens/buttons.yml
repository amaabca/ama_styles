# buttons.yml
props:
  button_background:
    value: "{!primary_color}"
  button--FontWeight:
    value: "bold"
  button--secondary--BackgroundColor:
    value: "{!secondary_color}"
  button--danger--BackgroundColor:
    value: "{!danger_color}"
  button--highlighted--BackgroundColor:
    value: "{!highlight_color}"
imports:
  - ./aliases.yml
global:
  type: color
  category: buttons
